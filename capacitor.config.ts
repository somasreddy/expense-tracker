import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.escriba.expensetracker',
  appName: 'ExpenseTracker',
  webDir: 'dist'
};

export default config;
